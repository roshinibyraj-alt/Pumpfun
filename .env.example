# All variables below are optional -- every one has a working default
# in app/config.py. Copy this file to .env and uncomment/edit only
# what you want to change.

# paper | live (only "paper" is implemented in this codebase)
# TRADING_MODE=paper

# Polymarket API bases -- only override for a custom gateway/mirror
# GAMMA_API_BASE=https://gamma-api.polymarket.com
# CLOB_API_BASE=https://clob.polymarket.com

# How often the main loop polls Polymarket's CLOB for prices (seconds)
# POLL_INTERVAL_SECONDS=1.0

# Starting paper-trading balance (single shared pool across all trades)
# STARTING_CAPITAL=2000

# The latest week is the walk-forward evaluation/training period. A longer
# warm-up history is fetched so daily indicators have enough completed bars.
# AI_BACKTEST_DAYS=7
# AI_WARMUP_DAYS=45

# Trade only signals with enough probability and agreement across timeframes.
# AI_MIN_CONFIDENCE=0.56
# AI_MIN_ALIGNMENT=0.35
# AI_MODEL_WEIGHT=0.70

# Order controls
# ORDER_SHARES=200
# ORDER_PRICE=0.45
# ORDER_TIMEOUT_SECONDS=30
# TAKER_FALLBACK_MAX_PRICE=0.60
# TP_PRICE=0.99
